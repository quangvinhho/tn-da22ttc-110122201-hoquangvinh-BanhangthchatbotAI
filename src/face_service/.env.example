# Database (giống backend Node.js)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=quanghungmobile

# Đường dẫn model
UNET_WEIGHTS=./weights/unet_face_best.pth

# Ngưỡng nhận diện (0.0 - 1.0), cao hơn = nghiêm ngặt hơn
RECOGNIZE_THRESHOLD=0.60

# Port Flask service
FACE_SERVICE_PORT=5001
